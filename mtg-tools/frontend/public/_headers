# Cloudflare Pages headers. Vite fingerprints everything under /assets, so
# the bytes behind a name never change — a year of immutable is safe, and
# index.html (never cached this way) swaps the names on deploy.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
