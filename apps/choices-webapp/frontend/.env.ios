# Native (Capacitor) builds: vite build --mode ios loads this over .env.
# Public values only. For dev against the preview stack, override in
# .env.ios.local (gitignored), e.g.:
#   VITE_API_URL=https://<preview-cloudfront-domain>/api
VITE_API_URL=https://choices.austinjuliuskim.com/api
