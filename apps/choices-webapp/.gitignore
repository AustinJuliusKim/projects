node_modules/
dist/
.aws-sam/
.env
.env.local
# Committed on purpose: CI's sam deploy reads the stack config from it,
# and it holds no secrets (VAPID private key is not saved here).
!samconfig.toml
*.log
.DS_Store
