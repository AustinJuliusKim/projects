# Stage A worker notes

- Baseline (main @ cd7dc66): backend `node --test` = 108 pass / 0 fail. `sam validate` clean. `sam validate --lint` reports one PRE-EXISTING finding on clean main: W1030 (CertificateArn default "" fails ACM ARN pattern). CI does not run lint. Criterion used for template tasks: `sam validate` clean + no NEW lint findings beyond the pre-existing W1030.
- Catalog count: brief §5 says "24 frozen + pairing_deleted" and §6 says "all 25 types", but the FROZEN vault catalog enumerates 25 types (9 core + 6 A + 3 B + 5 C + 2 D). Implemented 25 + pairing_deleted = 26 registry entries; Glue projection enum will carry all 26. Followed the frozen catalog as authoritative.
- suggestion_accepted is implemented pairing-scoped (catalog actor_role column says "role", vs "role/null" for suggestion_shown), so create-screen typeahead accepts (no pairing yet) are not tracked. If create-screen accepts matter, catalog would need actor_role "role/null" — flagged for review, not deviated.
- billing.mjs and push.mjs were not in the brief's §5 modified-file list but needed small additive touches: checkout sessions now carry metadata.plan (sub_started's plan is otherwise unavailable to the webhook; legacy sessions without it skip sub_started rather than fabricate), and push.mjs gained _setWebpushForTests so the push_sent success path is testable. Both follow existing house patterns.
- doClaimSeat's retry condition widened from ConditionalCheckFailedException to the existing lostWriteRace() helper (which also matches TransactionCanceledException with a ConditionalCheckFailed reason) — required because the seat_claimed outbox item makes every claim save transactional. Behavior otherwise unchanged (still exactly one retry).
- CreatePairingView.jsx was touched beyond the brief's §5 list (3 lines): without it, game_created/rematch `source` would always read "manual" — the fill4 provenance the frozen catalog specifies. api.js createPairing/rematch accept an optional source arg; PlayView tracks the same for rematch.
- fill4_swapped semantics: implemented as re-fill count within one FillMyFour mount (each re-roll replaces the previous 4). Per-choice manual edits after a fill are not counted — the inputs live in the parent and the catalog gives only {swap_count:int}.
- suggestion layers emitted today are "pair" and "places"; "trie" is reserved until the L2 trie ships.
- Brief §7 task 9's verification command (yaml.safe_load) rejects CFN short-form tags — it fails even on the ops/billing-alarms.yaml precedent. event-lake-alarms.yaml uses long-form intrinsics (Ref:) so the check genuinely passes; noted in the template header.
- Supervisor cycle-2 notes folded into docs/event-lake.md deletion story: late-writer edge (raw event after ref applied is never re-cleaned; practically unreachable, manual re-pend documented) and versioned-bucket delete markers (future ExpiredObjectDeleteMarker lifecycle rule).
- Task 10's "then PR" step was skipped on purpose: worker hard rules forbid git push / opening PRs. Ready-to-ship notes for whoever opens the PR: include the catalog-mapping table from docs/event-lake.md §catalog, flag the link_clicked/order_click double-emission for Austin's confirmation (brief §9.4), end with the Ops tasks section per repo CLAUDE.md (§8 of the brief: IAM apply, AnonSalt check, preview smoke, alarms deploy, Athena check, no-backfill decision), and run /vault-sync before shipping (repo CLAUDE.md; not run from this worker — it edits the vault outside the worktree).
- package-lock.json shows moderate transitive churn from `npm install` re-resolving @aws-sdk minors when adding @aws-sdk/util-dynamodb; npm ci + sam build both green on the resulting lock.
- Acceptance-review fix applied: ScheduleV2 event now has Name "${AWS::StackName}-compaction-weekly" (prod/preview collision + IAM scope match). Advisory: ops/event-lake-alarms.yaml is prod-only as written (fixed TopicName/AlarmNames per the billing-alarms precedent) — header now says so; parameterize names if preview alarms are ever wanted.
