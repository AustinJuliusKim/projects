# Lambda Function URL (from `sam deploy` output ApiUrl)
VITE_API_URL=https://xxxxxxxx.lambda-url.us-east-1.on.aws/

# VAPID public key (from `sam deploy` output VapidPublicKey)
VITE_VAPID_PUBLIC_KEY=
